# Index

# Index

## .

+   .netrc: 命令行泄露, .netrc, .netrc 文件格式, 启用 netrc

    ## /

+   /etc/hosts: 主机名或地址, 编辑 hosts 文件

    ## <

+   <curl curl.h="" class="calibre28">: include/curl, curl --libcurl, 头文件, 获取简单的 HTML 页面, 获取内存中的 HTML 页面

    ## A</curl>

+   --anyauth: HTTP 身份验证

+   apt: apt

    ## B

+   -b: Cookie 引擎, 从文件读取 cookie, 将 cookie 写入文件, 新的 cookie 会话, curl HTTP 速查表

+   --basic: HTTP 身份验证

+   BoringSSL: 选择 TLS 后端, 构建以使用 TLS 库, OpenSSL、BoringSSL、libressl, 使用 boringssl 构建 curl, 构建 boringssl, 设置构建树以被 curl 的配置检测到, 配置 curl

    ## C

+   -c: 将 cookie 写入文件, curl HTTP 速查表

+   c-ares: 使用 c-ares 的名称解析技巧, c-ares, 名称解析后端, 名称服务器选项

+   C89: 注释, 构建和安装

+   CA: 详细模式, MITM 代理, 可用的退出代码, 验证服务器证书, CA 存储, Windows 上的 CA 存储, OCSP stapling

+   Chrome: 复制为 curl, 从 Chrome

+   克隆: 克隆代码, git, 拉取请求, 网站源代码, 构建 boringssl

+   行为准则: 行为准则

+   --compressed: 压缩, Gzipped 传输, curl HTTP 技巧表

+   配置: 根, include/curl, 处理不同的构建选项, 在 Linux 和类 Unix 系统上, Autotools, 交叉编译, 选择 TLS 后端, 配置, OpenSSL, BoringSSL, libressl, GnuTLS, NSS, WolfSSL, axTLS, mbedTLS, Secure Transport, Schannel, 设置构建树以被 curl 的 configure 检测到, 配置 curl

+   --connect-timeout: 连接不超过此时间

+   --connect-to: 提供替代名称

+   连接缓存: 持久连接, 连接重用, 共享 "连接缓存"

+   连接池: 连接重用, 连接重用, 多 API 池, 共享 "连接缓存"

+   连接重用: 连接重用, 连接重用

+   内容编码: 压缩, 传输编码

+   贡献: 行为准则, 贡献, 拉取请求

+   贡献: 文档, 贡献

+   Cookie 引擎: Cookie 引擎, 写入 Cookies 到文件, Cookie 引擎, 启用读取的 Cookie 引擎, 启用写入的 Cookie 引擎, 设置自定义 Cookies

+   Cookies: 文档, 服务器差异, 更改 Host: 标头, 不完美, 使用 Cookies 保持状态, HTTP 认证, Cookies, Cookie 引擎, 从文件读取 Cookies, 写入 Cookies 到文件, 新的 Cookie 会话, libpsl, 默认简单，按需更多, 可用信息, 在 easy 句柄之间共享, 共享内容, 使用 libcurl 的 Cookies, Cookie 引擎, 启用读取的 Cookie 引擎, 启用写入的 Cookie 引擎, 设置自定义 Cookies, 导入导出, 从 Cookie 存储中获取所有 Cookies, Cookie 存储命令

+   版权: 许可证, 版权

+   curl-announce: curl 公告, 漏洞处理

+   curl-library: curl 用户, curl-library, 为邮件列表制作补丁, 漏洞处理

+   curl-users: curl 用户, 漏洞处理

+   CURLE_ABORTED_BY_CALLBACK: 进度回调

+   CURLMOPT_SOCKETFUNCTION: socket_callback

+   CURLMOPT_TIMERFUNCTION: 计时器回调

+   CURLOPT_CLOSEOCKETFUNCTION: 套接字关闭回调

+   CURLOPT_COOKIE: 设置自定义 cookie

+   CURLOPT_COOKIEFILE: 启用带读取功能的 cookie 引擎

+   CURLOPT_COOKIEJAR: 启用带写入功能的 cookie 引擎

+   CURLOPT_COOKIELIST: 将 cookie 添加到 cookie 存储, Cookie 存储命令

+   CURLOPT_CUSTOMREQUEST: 请求方法

+   CURLOPT_DEBUGDATA: 调试回调, 跟踪所有内容

+   CURLOPT_DEBUGFUNCTION: 调试回调, 跟踪所有内容

+   CURLOPT_DNS_CACHE_TIMEOUT: 缓存

+   CURLOPT_DNS_INTERFACE: 名称服务器选项

+   CURLOPT_DNS_LOCAL_IP4: 名称服务器选项

+   CURLOPT_DNS_LOCAL_IP6: 名称服务器选项

+   CURLOPT_DNS_SERVERS: 名称服务器选项

+   CURLOPT_DNS_USE_GLOBAL_CACHE: 全局 DNS 缓存不好

+   CURLOPT_ERRORBUFFER: curl --libcurl, CURLcode 返回代码

+   CURLOPT_FAILONERROR: 关于 HTTP 响应代码 "errors"

+   CURLOPT_HEADER: 写入回调, 引用页, 同时下载头部

+   CURLOPT_HEADERDATA: 头部回调, curl --libcurl, 同时下载头部

+   CURLOPT_HEADERFUNCTION: 头部回调, curl --libcurl

+   CURLOPT_HTTPGET: libcurl HTTP 下载

+   CURLOPT_HTTPHEADER: 添加一个头部, 更改一个头部, 移除一个头部, 提供一个没有内容的头部

+   CURLOPT_HTTPPOST: HTTP 多部分表单提交

+   CURLOPT_IPRESOLVE: 名称解析

+   CURLOPT_MAXFILE_LARGE: 设置数值选项

+   CURLOPT_MAXREDIRS: curl --libcurl

+   CURLOPT_NOBODY: 请求方法

+   CURLOPT_NOPROGRESS: 进度回调, curl --libcurl

+   CURLOPT_OPENSOCKETDATA: 提供文件描述符

+   CURLOPT_OPENSOCKETFUNCTION: 提供文件描述符

+   CURLOPT_POST: HTTP POST

+   CURLOPT_POSTFIELDS: 请求方法, 自定义 HTTP 请求头, HTTP POST

+   CURLOPT_POSTFIELDSIZE: HTTP POST

+   CURLOPT_POSTREDIR: 决定在重定向中使用的方法

+   CURLOPT_PROGRESSFUNCTION: 进度回调

+   CURLOPT_PROXY: 代理类型

+   CURLOPT_PROXYPORT: 代理类型

+   CURLOPT_PROXYTYPE: 代理类型

+   CURLOPT_READDATA: 读取回调, curl --libcurl

+   CURLOPT_READFUNCTION: 读取回调, curl --libcurl, HTTP POST

+   CURLOPT_RESOLVE: 主机的自定义地址

+   CURLOPT_SEEKDATA: curl --libcurl

+   CURLOPT_SEEKFUNCTION: curl --libcurl

+   CURLOPT_SOCKOPTDATA: sockopt 回调

+   CURLOPT_SOCKOPTFUNCTION: sockopt 回调

+   CURLOPT_SSH_KNOWNHOSTS: curl --libcurl

+   CURLOPT_STDERR: curl --libcurl, 详细操作

+   CURLOPT_TCP_KEEPALIVE: curl --libcurl

+   CURLOPT_TIMEOUT: 设置数值选项

+   CURLOPT_UPLOAD: 请求方法

+   CURLOPT_URL: 简单句柄, curl --libcurl, 设置句柄选项, 获取简单的 HTML 页面, 在内存中获取 HTML 页面, 请求方法, 自动引用, libcurl HTTP 下载, 同时下载头部

+   CURLOPT_USERAGENT: curl --libcurl, 在内存中获取 HTML 页面

+   CURLOPT_VERBOSE: 详细操作, 跟踪所有内容, 同时下载头部

+   CURLOPT_WRITEDATA: 写入回调, curl --libcurl, 在内存中获取 HTML 页面

+   CURLOPT_WRITEFUNCTION: 写入回调, curl --libcurl, 在内存中获取 HTML 页面

+   CURLOPT_XFERINFODATA: 进度回调

+   CURLOPT_XFERINFOFUNCTION: 进度回调

+   curl_easy_cleanup: 简单句柄, curl --libcurl, 获取简单的 HTML 页面, 在内存中获取 HTML 页面, 启用带写入的 cookie 引擎

+   curl_easy_init: 简单句柄, curl --libcurl, 获取简单的 HTML 页面, 在内存中获取 HTML 页面, libcurl HTTP 下载, 同时下载头部

+   curl_easy_perform: 使用简易接口进行驱动, 简易 API 池, 缓存, curl --libcurl, 获取简单的 HTML 页面, 在内存中获取 HTML 页面, 添加标头, 更改标头, 删除标头, 提供无内容的标头, 引用者, 自动引用者, libcurl HTTP 下载, 同时下载头部

+   curl_easy_reset: 简易句柄

+   curl_easy_setopt: 文档/libcurl/选项, 简单句柄, 写回调, 读回调, 进度回调, 头回调, 调试回调, sockopt 回调, 提供文件描述符, 套接字关闭回调, 名称解析, 主机的自定义地址, 简单句柄之间的共享, curl --libcurl, 设置句柄选项, 设置数值选项, 获取句柄选项, CURLcode 返回代码, 详细操作, 获取简单 HTML 页面, 在内存中获取 HTML 页面, 请求方法, 添加头, 更改头, 移除头, 无内容提供头, 引用者, 自动引用者, HTTP 范围, 启用带读取的 cookie 引擎, 启用带写入的 cookie 引擎, 设置自定义 cookie, 添加 cookie 到 cookie 存储, cookie 存储命令, libcurl HTTP 下载, 也下载头, HTTP POST, HTTP 多部分表单提交

+   curl_global_cleanup: 全局初始化, 在内存中获取 HTML 页面

+   curl_global_init: 全局初始化, 在内存中获取 HTML 页面

+   CURL_IPRESOLVE_V6: 名称解析

+   CURL_MAX_WRITE_SIZE: 写回调

+   curl_multi_add_handle: 使用多接口, 许多简单句柄

+   curl_multi_cleanup: 多 API

+   curl_multi_fdset: 使用多接口

+   curl_multi_info_read: 何时完成单个传输？, 何时完成？, 多 API

+   curl_multi_init: 使用多接口

+   curl_multi_remove_handle: 使用多接口, 许多简单句柄, 多 API

+   curl_multi_setopt: 文档/libcurl/选项, 使用多接口, socket_callback, timer_callback

+   curl_multi_socket_action: socket_callback, timer_callback, 如何启动所有操作, 何时完成？

+   curl_multi_timeout: 使用多接口

+   curl_multi_wait: 使用多接口

+   curl_off_t: 进度回调, 设置数值选项

+   CURL_SOCKET_TIMEOUT: timer_callback, 如何启动所有操作

+   curl_version_info: 运行哪个 libcurl 版本

    ## D

+   -d: 选项参数, 带空格的参数, 每个 URL 单独的选项, POST, HTTP 方法, HTTP POST, 内容类型, POST 二进制数据, 将其转换为 GET, 期望 100-continue, 分块编码的 POST 请求, 隐藏表单字段, -d vs -F, HTML 网页表单, HTML 之外的 POST 请求, PUT, curl HTTP 速查表

+   --data: 选项参数, 每个 URL 单独的选项, POST, HTTP POST, URL 编码

+   --data-binary: 不完美, POST 二进制数据, URL 编码

+   --data-urlencode: URL 编码

+   debian: lib/vtls, apt

+   调试回调: 调试回调, 详细操作

+   开发: 项目沟通, curl 用户, curl 库, 报告错误, 问题必须被知晓才能修复, 开发团队, 未来展望, 开发, GitHub 上的源代码, 谁决定什么进入代码库？, SSL 和 TLS 版本, 查看浏览器发送的内容, apt, yum, 运行的 libcurl 版本

    ## E

+   环境变量: 默认配置文件, 代理环境变量

+   礼仪: 邮件列表礼仪

+   事件驱动: 使用"multi_socket"接口进行驱动

    ## F

+   -F: 多部分表单提交, 不完美, HTTP 方法, 使用 curl 发送这样的表单, 内容类型, -d vs -F, HTML 网页表单, curl HTTP 速查表

+   Firefox: lib/vtls, 发现代理, 复制为 curl, 从 Firefox, 在 Firefox 上，不使用开发工具, 用户代理

+   Fragment: 片段, 锚点或片段

+   --ftp-method: multicwd, nocwd, singlecwd

+   --ftp-pasv: 被动连接

+   --ftp-port: 可用的退出代码, 活动连接

+   --ftp-skip-pasv-ip: 被动连接

+   future: 项目沟通, 未来, 文档, curl-security@haxx.se, 还有哪些其他协议？, HTTPS 到代理, "未使用", Cookies, 全局 DNS 缓存是不好的, API 兼容性

    ## G

+   --get: 将其转换为 GET

+   git: 每日快照, 克隆代码, 根目录, include/curl, 脚本, git, 拉取请求, 为邮件列表制作补丁, git 提交风格, 谁决定什么进入？, 网站源代码, 构建网站, git vs tarballs, 构建 boringssl

+   Globbing: URL 通配符, 通配符输出变量

+   GnuTLS: OCSP stapling, 构建以使用 TLS 库, GnuTLS, 代理类型

+   Gopher: 起源, curl 支持哪些协议？, GOPHER, 支持的协议

    ## H

+   --header: 服务器差异, 自定义头部

+   Header callback: 头部回调, HTTP 响应

+   homebrew: homebrew

+   Host:: 详细模式, --trace 和--trace-ascii, --trace-time, 修改 Host 头部, HTTP 协议基础知识, 转换为请求的 URL, HTTP 生成的内容, 自定义头部, 自定义 HTTP 请求头, 修改头部

+   HTTP 范围: HTTP 范围, HTTP 范围

+   HTTP 重定向: 简短选项, 长选项, 可用的退出码, HTTP 重定向, 告诉 curl 跟随重定向, 决定在重定向中使用的方法, 非 HTTP 重定向

+   HTTP/1.1: HTTP, 详细模式, --trace 和--trace-ascii, --trace-time, HTTP/2, HTTP 协议基础知识, HTTP 版本, HTTP 请求生成的内容, GET 还是 POST？, 请求方法, 自定义 HTTP 请求头, HTTP 版本

+   HTTP/2: 文档, HTTP, HTTP/2, 可用的退出代码, HTTP 版本, GET 还是 POST？, nghttp2, HTTP 版本

+   HttpGet: 它是如何开始的

    ## I

+   IDN: libidn2

+   Indentation: 缩进, 大括号在同一行

+   IPv4: 主机名或地址, 端口号, 可用的 --write-out 变量, 名称解析

+   IPv6: 主机名或地址, 端口号, URL 匹配, 可用的 --write-out 变量, 名称解析

    ## J

+   JavaScript: 客户端差异, PAC, JavaScript 和表单, JavaScript 重定向

+   json: 带空格的参数, Content-Type, HTML 之外的 POST

    ## K

+   -K: 命令行、引号和别名, 配置文件

+   keep-alive: 保持连接活动

+   --keepalive-time: 保持连接活动, 保持连接活动

    ## L

+   -L: 可用的 --write-out 变量, 告诉 curl 跟随重定向, 请求方法, Cookie 引擎, 从文件读取 cookies, curl HTTP 速查表

+   --libcurl: curl --libcurl

+   libcurl 版本: 可用的退出代码, 最新版本？, 哪个 libcurl 版本, 哪个 libcurl 版本在运行

+   libidn2: libidn2

+   libmetalink: libmetalink

+   libpsl: libpsl

+   libressl: 选择 TLS 后端, 构建使用 TLS 库, OpenSSL，BoringSSL，libressl

+   librtmp: librtmp

+   libssh2: libssh2

+   许可证: 查找用户, 知名用户, 许可证, 根

+   --limit-rate: 速率限制

+   --location: 长选项, 每个 URL 单独的选项, 配置文件, 告诉 curl 跟随重定向

    ## M

+   --max-filesize: 最大文件大���

+   --max-time: 重试失败的尝试, 允许的最大时间

+   --metalink: Metalink

+   Metalink: Metalink

+   MIT: 许可证

+   MITM 代理: MITM 代理

+   多线程: libcurl 多线程

    ## N

+   name resolving: 处理不同的构建选项, 主机名解析, 可用的--write-out 变量, c-ares 的名称解析技巧, SOCKS 类型, 连接重用, 名称解析, 名称解析器后端, 名称服务器选项, 代理类型, 可用信息, 名称解析

+   --negotiate: 网络泄漏, HTTP 身份验证

+   --netrc-file: 启用 netrc

+   --netrc-optional: 启用 netrc

+   nghttp2: nghttp2

+   nix: nix

+   --no-eprt: 主动连接

+   --no-epsv: 被动连接

+   NSS: OCSP stapling, yum, 构建使用 TLS 库, NSS, 代理类型

+   --ntlm: 网络泄露、HTTP 身份验证

    ## O

+   -O: 许多选项和 URL、数字范围、字母范围、列表、组合、用 URL 命名的文件下载、从服务器获取目标文件名、Shell 重定向、多个下载、对所有 URL 使用 URL 的文件名部分、恢复和范围、curl HTTP 速查表

+   openldap: openldap

+   OpenSSL: lib/vtls、OCSP stapling、选择 TLS 后端、构建使用 TLS 库、配置、OpenSSL、BoringSSL、libressl、代理类型、可用信息

    ## P

+   PAC: PAC

+   Percent-encoding: URL 编码

+   pop3: curl 支持哪些协议？、POP3、POP3S、无协议、支持的协议、详细模式、可用的退出码、读取电子邮件、POP3、安全邮件传输、启用 TLS

+   端口号: 连接到“端口号”, 端口号, 可用的--write-out 变量, 提供替换名称, 本地端口号, HTTP, HTTP 代理隧道, 可用的退出代码, 转换为请求的 URL, 连接重用, 主机的自定义地址, 代理, 代理类型, 传输后信息

+   --post301: 决定在重定向中使用的方法

+   --post302: 决定在重定向中使用的方法

+   --post303: 决定在重定向中使用的方法

+   进度回调: timer_callback, 进度回调

+   proxy: 起源, 可用的--write-out 变量, 中介的操作, 代理, 发现你的代理, PAC, 代理类型, HTTP, HTTPS 和代理, MITM 代理, 非 HTTP 协议通过 HTTP 代理, HTTP 代理隧道, SOCKS 类型, 代理身份验证, HTTPS 到代理, 代理环境变量, 代理头部, 可用的退出码, CONNECT 响应码, 传递传输编码, HTTP 身份验证, 代理, 代理类型, 本地或代理名称查找, HTTP 代理, HTTPS 代理, 代理身份验证, 可用信息, HTTP 代理

+   --proxy: HTTP, HTTP 身份验证

+   --proxy-user: 代理身份验证, HTTP 身份验证

+   --proxy1.0: HTTP 代理隧道

+   --proxytunnel: HTTP 代理隧道

    ## R

+   ranges: 数字范围, 字母范围, 组合, 恢复和范围, HTTP 范围, 提供文件描述符, HTTP 响应码, 关于 HTTP 响应码"错误", HTTP 范围

+   Read callback: 读取回调, HTTP POST

+   redhat: lib/vtls, yum

+   重定向：长选项，每个 URL 的独立选项，配置文件，可用的--write-out 变量，下载到 URL 命名的文件，Shell 重定向，为名称提供自定义 IP 地址，提供替换名称，可用的退出代码，自动跟随重定向，HTTP 重定向，永久和临时，告诉 curl 自动跟随重定向，GET 还是 POST？，决定在重定向中使用的方法，非 HTTP 重定向，HTML 重定向，JavaScript 重定向，请求方法，主机的自定义地址，可用信息，自动引荐者

+   RELEASE-NOTES：脚本

+   发布：1\. cURL 项目)，curl-announce，发布，脚本，详细模式，libcurl 版本

+   --remote-name-all：对所有 URL 使用 URL 的文件名部分

+   仓库：发布，每日快照，GitHub 上的源代码，托管和下载，根，include/curl，脚本，要添加什么，拉取请求，谁决定加入什么？，网站源代码，构建网页，从包存储库安装，git 与 tarballs

+   --resolve：为名称提供自定义 IP 地址，提供替换名称

+   RTMP：curl 支持哪些协议？，RTMP，支持的协议，librtmp

+   RTSP：curl 支持哪些协议？，RTSP，支持的协议，RTSP 交错回调，可用信息

    ## S

+   Schannel：Windows 上的 CA 存储，构建以使用 TLS 库，Schannel

+   Scheme：连接到 "端口号"，FILE，Scheme，方案分隔符，无方案，用户名和密码，代理类型，SOCKS 类型，代理身份验证，可用的退出代码，librtmp，代理类型，可用信息，哪个 libcurl 版本，绑定

+   SCP：curl 支持哪些协议？，SCP，支持的协议，允许上传的协议，可用的退出代码，SCP 和 SFTP，URL，已知主机，libssh2

+   安全性: curl-announce, 安全性, 过去的安全问题, 信任, 文档, 报告漏洞, 漏洞处理, TLS, 协议变化多少？, FTPS, TLS, 密码, 启用 TLS, 如何使用 curl 进行 HTTP

+   SFTP: curl 支持哪些协议？, 关于遵循标准和谁是正确的, SFTP, 支持的协议, --trace 和 --trace-ascii, 允许上传的协议, 可用的退出代码, SCP 和 SFTP, URL, 已知主机, libssh2

+   --show-error: 静音模式

+   --silent: 进度表, 静音模式, 错误消息

+   SMTP: curl 支持哪些协议？, SMTP, SMTPS, 无协议, 支持的协议, 详细模式, 允许上传的协议, SMTP 上传, 可用的退出代码, SMTP, 安全邮件传输, SMTP URL, 启用 TLS

+   SMTPS: curl 支持哪些协议？, SMTPS, 支持的协议, 允许上传的协议, 启用 TLS, 构建以使用 TLS 库

+   快照: 每日快照, 根

+   SNI: 修改 Host 标头，为名称提供自定义 IP 地址

+   --socks4: SOCKS 类型

+   --socks4a: SOCKS 类型

+   --socks5: SOCKS 类型

+   --socks5-hostname: SOCKS 类型

+   --speed-limit: 传输速度慢于此意味着退出

+   --speed-time: 传输速度慢于此意味着退出

+   SSH: SCP，SFTP，可用的退出码，SCP 和 SFTP，已知主机，SSH 密钥回调，跟踪所有内容

+   SSL 上下文回调: SSL 上下文回调

    ## T

+   -T: PUT，FTP 上传，SMTP 上传，使用 FTP 上传，HTTP 方法，PUT，curl HTTP 快速参考表

+   TELNET: curl 支持哪些协议？，TELNET，支持的协议，可用的退出码，TELNET

+   测试: curl 做了什么？，报告错误，测试，处理不同的构建选项，贡献，关于 HTTP 响应码“错误”

+   TLS: lib/vtls, 文档, 处理不同的构建选项, TLS, 传输数据, 协议会改变多少？, 连接重用, 详细模式, 更改 Host: 标头, MITM 代理, 可用的退出代码, SCP 和 SFTP, 已知主机, TLS, 密码, 启用 TLS, SSL 和 TLS 版本, 验证服务器证书, CA 存储, Windows 上的 CA 存储, 证书钉定, OCSP stapling, 客户端证书, TLS auth, 不同的 TLS 后端, 多个 TLS 后端, 如何使用 curl 进行 HTTP, 将 URL 转换为请求, HTTPS, 弄清楚浏览器发送了什么, apt, yum, 选择 TLS 后端, TLS 库, 构建以使用 TLS 库, 代理类型, 可用信息, 跟踪所有内容

+   TODO: 建议

+   --tr-encoding: 压缩, 传输编码

+   --trace: --trace 和 --trace-ascii, --trace-time

+   --trace-ascii: --trace 和 --trace-ascii, --trace-time, 服务器差异, curl HTTP 速查表

+   --trace-time: --trace-time

+   传输编码：传递传输编码，分块编码的 POST 请求

    ## U

+   -U：代理身份验证

+   -u：密码和窥探，命令行泄露，身份验证，URLs，HTTP 身份验证，curl HTTP 速查表

+   URL 通配：URL 通配

    ## V

+   --verbose：长选项，详细模式，--trace-time

+   漏洞：漏洞处理

    ## W

+   写回调：写回调，在内存中获取 HTML 页面，HTTP 响应

+   --write-out：--write-out，可用的--write-out 变量，HTTP 响应代码，CONNECT 响应代码

    ## X

+   -x：HTTP，HTTPS 和代理，通过 HTTP 代理传输非 HTTP 协议，HTTP 代理隧道，SOCKS 类型，代理身份验证，代理环境变量，curl HTTP 速查表

+   -X：请求方法，请求目标，PUT，curl HTTP 速查表

    ## Y

+   yum：yum

    ## Z

+   zlib：zlib
