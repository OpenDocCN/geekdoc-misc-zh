# 评估条件语句

到目前为止，我们的语言只有算术。基于神秘语言：条件语句，我们现在将探讨如何扩展我们的语言以支持条件语句。现实语言中可能有相当复杂的条件表达式，但就我们的目的而言，有一个包含三个部分（条件、then 分支和 else 分支）的 `if` 语句就足够了。稍后，当我们学习如何扩展语言时，我们可以看到如何在这个基础上叠加更复杂的条件表达式。

在 SImPl 中，我们至少需要做两件事：

1.  将表示表达式的数据类型扩展以包括条件语句。

1.  将评估器扩展以处理（这些新表达式的）表示。

可选地，如果我们有一个解析器，我们还应该

1.  将解析器扩展以生成这些新表示。
